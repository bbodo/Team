<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Menu">  
  
   <!-- 메뉴 목록 가져오기 -->
   <select id="MenuList" resultType="com.green.menus.vo.MenuVo">
      SELECT  MENU_ID, MENU_NAME, MENU_SEQ
      FROM    MENUS
   </select>
   
   <!-- 서브메뉴 목록 가져오기 -->
   <select id="SubmenuList" resultType="com.green.menus.vo.SubmenuVo">
      SELECT  SUBMENU_ID, SUBMENU_NAME,  SUBMENU_SEQ
      FROM    SUBMENUS
      WHERE   MENU_ID = #{menu_id}
   </select>
   

   <insert id="UserWrite">
   
	INSERT INTO
		MEMBER
		(
			USERCODE,
			USERID,
			PASSWD,
			USERNAME,
			BIRTHDAY,
			GENDER,
			GRADE,
			ADDR,
			EMAIL,
			POINT
		)
		VALUES
		(
			( SELECT NVL( MAX(USERCODE ), 0 ) + 1 FROM MEMBER),
			#{ userid },
			#{ passwd },
			#{ username },
			#{ birthday },
			#{ gender },
			#{ grade },
			#{ addr },
			#{ email },
			500
		)
		
   
   </insert>
<!--    
   <delete id="UserDelete">
   
      DELETE  FROM   TUSER
        WHERE  USERID = #{ userid }  
           
   </delete>
 -->
</mapper>





   